/**
 * Feature flag: when true, the interactive SVG "growing tree" canvas is
 * disabled and the collapsible list view (originally built for mobile) is
 * used on every screen size instead.
 */
export var DISABLE_TREE_VIEW = false
